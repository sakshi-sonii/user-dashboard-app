specVersion: "v1beta"
serviceId: "user-dashboard-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "user-dashboard-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
